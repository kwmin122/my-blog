---
phase: 7
plan: 01
title: Content schema validation + 5 posts + alt sidecar JSON
wave: 1
depends_on: []
files_modified:
  - lib/validate-posts.ts
  - next.config.ts
  - content/posts/sample.mdx
  - content/posts/sample.alt.json
  - content/posts/post-diary-01.mdx
  - content/posts/post-diary-01.alt.json
  - content/posts/post-study-01.mdx
  - content/posts/post-study-01.alt.json
  - content/posts/post-log-01.mdx
  - content/posts/post-log-01.alt.json
  - content/posts/post-diary-02.mdx
  - content/posts/post-diary-02.alt.json
  - lib/waypoints.ts
requirements_addressed:
  - CONT-02
  - CONT-03
gap_closure: false
---

<objective>
Build the content data contract: category enum enforcement, minimum post count gate (5 posts with all three categories), alt sidecar JSON for every post, and waypoint entries for every new post slug.

Requirements fulfilled: CONT-02, CONT-03
Phase goal contribution: Establishes the data contract (schema, slugs, alt data) that the SR mirror in Plan 02 and the minimal mode content in Plan 03 both consume. Without concrete post slugs and alt JSON, those plans cannot be fully implemented.
Complexity: M
</objective>

<tasks>
<task id="7-01-01" type="auto">
  <name>Create lib/validate-posts.ts with validatePostsMeta and getAltData</name>
  <files>
    - lib/validate-posts.ts
  </files>
  <read_first>
    - .planning/phases/07-content-a11y-polish--cognitive-toggle/07-CONTEXT.md
    - .planning/phases/07-content-a11y-polish--cognitive-toggle/07-RESEARCH.md
    - lib/posts.ts
  </read_first>
  <action>
    Create a new file `lib/validate-posts.ts`. This file must NOT have `import 'server-only'` — it is called from `next.config.ts` (plain Node.js context) and the `server-only` package throws outside the Next.js server runtime.

    The file exports two functions and two interfaces:

    ```typescript
    import { readdirSync, readFileSync, existsSync } from 'node:fs'
    import { join } from 'node:path'

    const VALID_CATEGORIES = ['일기', '공부', '일지'] as const
    export type ValidCategory = typeof VALID_CATEGORIES[number]

    export interface AltVisual {
      id: string
      alt: string
    }

    export interface AltData {
      visuals: AltVisual[]
    }

    export function validatePostsMeta(): void {
      const postsDir = join(process.cwd(), 'content', 'posts')
      const slugs = readdirSync(postsDir)
        .filter((f) => f.endsWith('.mdx'))
        .map((f) => f.replace(/\.mdx$/, ''))

      if (slugs.length < 5) {
        throw new Error(
          `[CONT-02] posts ${slugs.length}/5 — 5편 이상 필요. 현재: ${slugs.join(', ')}`
        )
      }

      const counts: Record<string, number> = {}
      for (const slug of slugs) {
        const text = readFileSync(join(postsDir, `${slug}.mdx`), 'utf-8')
        const m = text.match(/category:\s*['"](.+?)['"]/)
        const cat = m?.[1]
        if (!cat || !(VALID_CATEGORIES as readonly string[]).includes(cat)) {
          throw new Error(
            `[CONT-02] ${slug}: category '${cat ?? '(없음)'}' 유효하지 않음. 허용값: ${VALID_CATEGORIES.join(', ')}`
          )
        }
        if (!existsSync(join(postsDir, `${slug}.alt.json`))) {
          throw new Error(`[CONT-03] ${slug}: ${slug}.alt.json 누락`)
        }
        counts[cat] = (counts[cat] ?? 0) + 1
      }

      for (const cat of VALID_CATEGORIES) {
        if (!counts[cat]) {
          throw new Error(`[CONT-02] '${cat}' 카테고리 글 없음. 각 카테고리 1편 이상 필요.`)
        }
      }
    }

    export function getAltData(slug: string): AltData {
      const altPath = join(process.cwd(), 'content', 'posts', `${slug}.alt.json`)
      if (!existsSync(altPath)) return { visuals: [] }
      const raw = readFileSync(altPath, 'utf-8')
      const parsed = JSON.parse(raw) as Partial<AltData>
      return { visuals: parsed.visuals ?? [] }
    }
    ```

    Note: `getAltData` returns `{ visuals: [] }` gracefully when the alt JSON is absent — this allows it to be used safely by server components even for slugs that haven't yet been paired with alt JSON during development.
  </action>
  <acceptance_criteria>
    - lib/validate-posts.ts exists
    - lib/validate-posts.ts contains 'export function validatePostsMeta()'
    - lib/validate-posts.ts contains 'export function getAltData('
    - lib/validate-posts.ts contains 'export interface AltVisual'
    - lib/validate-posts.ts contains 'export interface AltData'
    - lib/validate-posts.ts contains "VALID_CATEGORIES = ['일기', '공부', '일지']"
    - lib/validate-posts.ts does NOT contain "import 'server-only'"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task id="7-01-02" type="auto">
  <name>Wire validatePostsMeta into next.config.ts build gate</name>
  <files>
    - next.config.ts
  </files>
  <read_first>
    - .planning/phases/07-content-a11y-polish--cognitive-toggle/07-CONTEXT.md
    - next.config.ts
    - lib/validate-posts.ts
  </read_first>
  <action>
    Modify `next.config.ts` to import `validatePostsMeta` from `./lib/validate-posts` and call it during `next build` (not during `next dev`).

    Add at the top of the file, after the existing imports:

    ```typescript
    import { validatePostsMeta } from './lib/validate-posts'

    // Run content validation during production builds only.
    // NEXT_PHASE is set to 'phase-development-server' during `next dev`.
    // During `next build` it is set to 'phase-production-build'.
    if (process.env.NEXT_PHASE !== 'phase-development-server') {
      validatePostsMeta()
    }
    ```

    The final file structure must preserve the existing `withMDX` wrapper:

    ```typescript
    import type { NextConfig } from 'next'
    import createMDX from '@next/mdx'
    import { validatePostsMeta } from './lib/validate-posts'

    if (process.env.NEXT_PHASE !== 'phase-development-server') {
      validatePostsMeta()
    }

    const nextConfig: NextConfig = {
      pageExtensions: ['ts', 'tsx', 'js', 'jsx', 'md', 'mdx'],
      turbopack: {},
      webpack: (config, { isServer }) => {
        if (isServer) {
          config.resolve.alias = {
            ...config.resolve.alias,
            'three/webgpu': false,
          }
        }
        return config
      },
    }

    const withMDX = createMDX({})

    export default withMDX(nextConfig)
    ```
  </action>
  <acceptance_criteria>
    - next.config.ts contains "import { validatePostsMeta } from './lib/validate-posts'"
    - next.config.ts contains "process.env.NEXT_PHASE !== 'phase-development-server'"
    - next.config.ts contains 'validatePostsMeta()'
    - next.config.ts still contains 'withMDX(nextConfig)'
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task id="7-01-03" type="auto">
  <name>Fix sample.mdx category and update sample.alt.json schema</name>
  <files>
    - content/posts/sample.mdx
    - content/posts/sample.alt.json
  </files>
  <read_first>
    - .planning/phases/07-content-a11y-polish--cognitive-toggle/07-CONTEXT.md
    - content/posts/sample.mdx
    - content/posts/sample.alt.json
  </read_first>
  <action>
    **content/posts/sample.mdx:**
    Change `category: '탐험'` to `category: '일기'`. This is the mandatory fix from D-01a. Keep all other content unchanged.

    The metadata block should become:
    ```
    export const metadata = {
      title: '첫 번째 글',
      excerpt: '이것은 월드에서 탐험할 수 있는 첫 번째 글입니다.',
      date: '2026-04-14',
      category: '일기',
    }
    ```

    **content/posts/sample.alt.json:**
    Remove the vestigial `"slug"` field (D-02b: top-level `"slug"` is vestigial). Add at least one visual entry with a meaningful id and alt string for the sample island. Final content:

    ```json
    {
      "visuals": [
        { "id": "sample-island", "alt": "구름 위에 떠 있는 작은 섬과 뾰족한 나무 한 그루" }
      ]
    }
    ```
  </action>
  <acceptance_criteria>
    - content/posts/sample.mdx contains "category: '일기'"
    - content/posts/sample.mdx does NOT contain "category: '탐험'"
    - content/posts/sample.alt.json does NOT contain '"slug"'
    - content/posts/sample.alt.json contains '"visuals"'
    - content/posts/sample.alt.json contains '"sample-island"'
  </acceptance_criteria>
</task>

<task id="7-01-04" type="auto">
  <name>Create 4 new posts (MDX + alt JSON) to reach minimum 5</name>
  <files>
    - content/posts/post-diary-02.mdx
    - content/posts/post-diary-02.alt.json
    - content/posts/post-study-01.mdx
    - content/posts/post-study-01.alt.json
    - content/posts/post-log-01.mdx
    - content/posts/post-log-01.alt.json
    - content/posts/post-diary-01.mdx
    - content/posts/post-diary-01.alt.json
  </files>
  <read_first>
    - .planning/phases/07-content-a11y-polish--cognitive-toggle/07-CONTEXT.md
    - content/posts/sample.mdx
    - content/posts/sample.alt.json
  </read_first>
  <action>
    Create 4 new MDX posts and their sibling alt JSON files. Post content is at author's discretion (D-04 Claude's Discretion). Use the same `export const metadata = { title, excerpt, date, category }` pattern from sample.mdx.

    Category distribution after all 5 posts:
    - `일기`: sample (reclassified), post-diary-01, post-diary-02 → 3 posts
    - `공부`: post-study-01 → 1 post
    - `일지`: post-log-01 → 1 post
    Total: 5 posts, all three categories covered.

    **content/posts/post-diary-01.mdx:**
    ```mdx
    export const metadata = {
      title: '오늘 하루 기록',
      excerpt: '작은 발견들을 글로 남기는 일상의 단편.',
      date: '2026-04-15',
      category: '일기',
    }

    # 오늘 하루 기록

    작은 발견들을 글로 남기는 일상의 단편.

    3D 월드를 만들면서 느낀 것들을 기록해두기로 했다. 코드 한 줄 한 줄이 섬 하나씩 쌓여가는 느낌이다.
    ```

    **content/posts/post-diary-01.alt.json:**
    ```json
    {
      "visuals": [
        { "id": "diary-island", "alt": "일기를 쓰는 책상이 놓인 작고 조용한 섬" }
      ]
    }
    ```

    **content/posts/post-diary-02.mdx:**
    ```mdx
    export const metadata = {
      title: '구름 위에서',
      excerpt: '월드를 구경하다 문득 느낀 고요함에 대하여.',
      date: '2026-04-16',
      category: '일기',
    }

    # 구름 위에서

    월드를 구경하다 문득 느낀 고요함에 대하여.

    구름 위에 떠 있는 섬을 카메라가 천천히 비행할 때, 멈추고 싶지 않았다.
    ```

    **content/posts/post-diary-02.alt.json:**
    ```json
    {
      "visuals": [
        { "id": "cloud-view", "alt": "넓게 펼쳐진 구름 바다 위를 가로지르는 카메라 시점" }
      ]
    }
    ```

    **content/posts/post-study-01.mdx:**
    ```mdx
    export const metadata = {
      title: 'WebGPU와 TSL 셰이더 공부',
      excerpt: 'Three.js Shading Language로 첫 셰이더를 작성한 기록.',
      date: '2026-04-15',
      category: '공부',
    }

    # WebGPU와 TSL 셰이더 공부

    Three.js Shading Language로 첫 셰이더를 작성한 기록.

    TSL은 WGSL과 GLSL을 동시에 타겟으로 컴파일해준다. 노드 그래프 방식으로 셰이더를 작성하면 WebGPU 환경과 WebGL2 폴백 모두에서 동일한 시각 결과를 낼 수 있다.
    ```

    **content/posts/post-study-01.alt.json:**
    ```json
    {
      "visuals": [
        { "id": "study-island", "alt": "모니터와 책이 가득한 공부방 섬, 코드가 빛나는 CRT 화면" }
      ]
    }
    ```

    **content/posts/post-log-01.mdx:**
    ```mdx
    export const metadata = {
      title: 'Phase 7 작업 일지',
      excerpt: '접근성 레이어와 콘텐츠 스키마를 완성한 날의 기록.',
      date: '2026-04-16',
      category: '일지',
    }

    # Phase 7 작업 일지

    접근성 레이어와 콘텐츠 스키마를 완성한 날의 기록.

    SR-only DOM 미러를 구현하면서 3D 레이어와 접근성 레이어를 DOM 계층에서 명시적으로 분리하는 게 얼마나 중요한지 다시 한번 실감했다.
    ```

    **content/posts/post-log-01.alt.json:**
    ```json
    {
      "visuals": [
        { "id": "log-island", "alt": "작업 노트가 흩어진 개발자의 섬, 형광등이 켜진 작업대" }
      ]
    }
    ```
  </action>
  <acceptance_criteria>
    - content/posts/post-diary-01.mdx exists
    - content/posts/post-diary-01.alt.json exists
    - content/posts/post-diary-02.mdx exists
    - content/posts/post-diary-02.alt.json exists
    - content/posts/post-study-01.mdx exists
    - content/posts/post-study-01.alt.json exists
    - content/posts/post-log-01.mdx exists
    - content/posts/post-log-01.alt.json exists
    - content/posts/post-diary-01.mdx contains "category: '일기'"
    - content/posts/post-diary-02.mdx contains "category: '일기'"
    - content/posts/post-study-01.mdx contains "category: '공부'"
    - content/posts/post-log-01.mdx contains "category: '일지'"
    - All 4 alt JSON files contain '"visuals"'
  </acceptance_criteria>
</task>

<task id="7-01-05" type="auto">
  <name>Add waypoint entries in lib/waypoints.ts for new post slugs</name>
  <files>
    - lib/waypoints.ts
  </files>
  <read_first>
    - .planning/phases/07-content-a11y-polish--cognitive-toggle/07-CONTEXT.md
    - lib/waypoints.ts
    - components/world/ArchipelagoScene.tsx
  </read_first>
  <action>
    Add waypoint entries for the 4 new post slugs to `lib/waypoints.ts`. Reuse existing island positions (no new geometry needed — this maps new post slugs to the 3 existing non-home island positions). The approach follows the research finding: "map new post slugs to existing SCROLL_WAYPOINTS positions rather than adding new geometry."

    Add to the `WAYPOINTS` record the following entries:

    ```typescript
    'post-diary-01': {
      slug: 'post-diary-01',
      position: { x: -8, y: 3, z: 0 },
      target: { x: -8, y: 0, z: 0 },
      islandIndex: 1,
    },
    'post-diary-02': {
      slug: 'post-diary-02',
      position: { x: 8, y: 3, z: 0 },
      target: { x: 8, y: 0, z: 0 },
      islandIndex: 2,
    },
    'post-study-01': {
      slug: 'post-study-01',
      position: { x: 8, y: 3, z: 0 },
      target: { x: 8, y: 0, z: 0 },
      islandIndex: 2,
    },
    'post-log-01': {
      slug: 'post-log-01',
      position: { x: 0, y: 3, z: -10 },
      target: { x: 0, y: 0, z: -10 },
      islandIndex: 3,
    },
    ```

    These are additive entries in the existing `WAYPOINTS` record — do not modify or remove the `home` or `sample` entries, and do not modify `SCROLL_WAYPOINTS`.
  </action>
  <acceptance_criteria>
    - lib/waypoints.ts contains "'post-diary-01'"
    - lib/waypoints.ts contains "'post-diary-02'"
    - lib/waypoints.ts contains "'post-study-01'"
    - lib/waypoints.ts contains "'post-log-01'"
    - lib/waypoints.ts still contains "'home'"
    - lib/waypoints.ts still contains "'sample'"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- [ ] lib/validate-posts.ts exists with validatePostsMeta() and getAltData() exported
- [ ] lib/validate-posts.ts has no 'server-only' import
- [ ] next.config.ts calls validatePostsMeta() gated on NEXT_PHASE !== 'phase-development-server'
- [ ] content/posts/sample.mdx has category: '일기' (not '탐험')
- [ ] content/posts/sample.alt.json has no top-level "slug" field and has at least one visual entry
- [ ] 4 new MDX posts exist: post-diary-01, post-diary-02, post-study-01, post-log-01
- [ ] All 5 posts have sibling .alt.json files with at least one visual entry
- [ ] Categories covered: 일기 × 3, 공부 × 1, 일지 × 1
- [ ] lib/waypoints.ts has WAYPOINTS entries for all 4 new slugs
- [ ] All task acceptance criteria verified
- [ ] /sunco:lint passes with zero errors
- [ ] npx tsc --noEmit passes with zero errors
- [ ] pnpm build exits 0 (validates build-time throws work)
</done_when>
