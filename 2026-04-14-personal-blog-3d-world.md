# Design: 개인 블로그 — Single Continuous 3D World

Generated by /sunco:office-hours on 2026-04-14
Branch: master
Status: APPROVED (2026-04-14, post adversarial review)
Mode: Builder (with Startup discipline overlay — "독자 확보" 부 목표)
Owner: 민경욱

---

## Problem Statement

개인 블로그를 만든다. 단, 일반적인 블로그가 아니라 **사이트 자체가 작품**인 곳. 주 목적은:

1. **기술력 + 디자인 취향** 양쪽을 즉시 증명하는 디지털 명함·포트폴리오
2. (부) 글 발행 / 독자 확보 — 단, 글이 일기·공부·일지 수준이라 본질적으로 *사이트 임팩트가 트래픽 엔진*
3. (부) 2026년 핵심 웹 트렌드(WebGPU·모핑 스크롤·도파민 컬러·리퀴드 글래스·뉴트로 등)를 *한 사이트에 다 녹인* 작품

레퍼런스 자료: `Desktop/dev/llm-wiki-webdesign/wiki/syntheses/2026-web-design-trends.md` 및 `wiki/concepts/*`.

---

## What Makes This Cool

- 첫 방문자가 페이지가 아니라 **세계**에 도착한다. 카메라가 떠오르고, 풍경이 보이고, 어딘가로 비행해 들어간다.
- 글 한 편 = 페이지 한 장이 아니라 **월드 안의 한 장소**. URL을 공유하면 카메라가 그 장소로 날아간다.
- 도파민 컬러·리퀴드 글래스·뉴트로·커스텀 커서 같은 트렌드 요소들이 *데모로 나열된 게 아니라*, 월드 안의 오브젝트·표지판·UI 패널로 자연스럽게 살아 있다.
- 글이 거의 없어도 사이트가 가난해 보이지 않는다 — 월드가 콘텐츠 자리를 메운다.
- 친구한테 보내면 "이게 뭐야 진짜 블로그야?" 라는 반응이 나온다.

---

## Constraints

| 항목 | 결정 |
|---|---|
| 작업자 | 1인 (민경욱) — MILK 백엔드·WalkMate·학기 과제와 시간 경합 |
| 제작 방식 | 바이브 코딩 (AI 보조) 우선, 단 **셰이더·perf·디자인 시스템은 직접 학습** |
| 데드라인 | 사용자 명시 "기한 없음" — **office hours 권고: 자체 부과 v0.5/v1.0 마일스톤 설정** (아래 Success Criteria 참조) |
| 디바이스 우선순위 | **데스크톱 1순위** 풀 경험 / **모바일 = 우아한 정적 폴백** (3D 정지 이미지 + 단순 텍스트 라우트) |
| "기술 다 넣기" 해석 | **트렌드 가지치기 금지** — v1.0 출시 시점에 *모든* 합의 트렌드가 통합되어 있어야 함. 단, 출시 *과정*은 이터레이티브 (v0.1→v0.5→v1.0). 즉 "릴리즈 단계화 OK, 트렌드 단계화 NO". (오리지널 문구 "단계화 거부"는 후자만을 의미했음. 명시 분리.) |
| 주당 가용시간 추정 | 8~12h (MILK 백엔드·WalkMate·학기·창업과 분할). v1.0 현실적 데드라인 = **2026-12-31 (8개월)**. 무기한 금지. |
| 제외 | 에이전틱 AI / 동적 레이아웃 재배치 — ROI 0, 비용·보안·서버 부담 |

---

## Premises (사용자 검증 완료)

1. ✅ "독자 확보"는 사실상 부차 목표. 사이트 임팩트가 트래픽 엔진. 글 UX 과투자 금지.
2. ⚠️ "기술 다 넣기" = **트렌드 가지치기 금지** (v1.0 시점에 모두 통합). *릴리즈 단계화*는 별개 — v0.1/v0.5/v1.0 마일스톤은 같은 트렌드 셋을 점진적으로 켜는 것. 두 개념을 구분해 운영. 위험 완화 = "월드"라는 통합 컨텍스트.
3. ✅ 바이브 코딩은 80%까지 빠르고 마지막 20%(perf·일관성·접근성)에서 무너진다. → **성능 게이트·디자인 토큰·a11y 최소선**을 plan에 강제.
4. ✅ 무기한 = 못 끝낸다. → **자체 부과 마일스톤** 박음 (v0.5/v1.0/v1.5).
5. ✅ 모바일 1순위 아님. 우아한 폴백 전략.
6. ✅ 에이전틱 AI 제외.

---

## Approaches Considered

### A. Stack-on-Shoulders (라이브러리 적층)
- Astro + R3F + Drei + Tailwind v4 + GSAP + Lenis + Rive + Spline + glass-ui + MDX
- Pros: 빠른 통합, 바이브 코딩 적합, 도구별 베스트프랙티스 풍부
- Cons: 디자인 일관성은 큐레이션 의존 → 데모 잡탕 위험. 성능 = 예산의 합 → 모바일 사실상 포기. R3F + WebGPU 호환성 의존.
- Risk: Med-High

### B. Custom Atelier (수공예 통합)
- Next.js + 자체 Three.js/TSL 셰이더 + 자체 디자인 시스템 + 자체 GSAP 타임라인 + 자작 Liquid Glass 셰이더
- Pros: 디자인 무결성 100%, 성능 월드 클래스, 학습 자산 최대
- Cons: 6~12개월 본격 작업, 바이브 코딩이 가장 안 통하는 영역, "다 넣기" 욕구와 충돌
- Risk: High (시간), Reward: 최고 (희소성)

### C. Single Continuous World (lateral) ★ 선택
- 사이트 = 하나의 WebGPU 3D 월드. 카메라가 월드 안 장소들 사이를 스크롤 비행. 글·프로젝트 = 월드 안의 장소. 트렌드 요소 = 월드 안의 오브젝트·UI 패널.
- Tech: R3F + WebGPURenderer + Drei + Theatre.js (또는 GSAP 카메라 타임라인) + Lenis + zustand + drei `<Html>` (월드 안 MDX) + Astro/Next.js 사이드카(SEO·접근성 폴백)
- Pros: "사이트 자체가 작품" 1:1 매칭. "기술 다 넣기"가 *세계관 디테일*로 정당화. 글 빈도 낮아도 OK.
- Cons: 모바일·SEO·a11y 가장 어려움. 월드 컨셉 결정이 80% 작업. 컨셉 피벗 비용 큼.
- Risk: High, Reward: 최고

---

## Recommended Approach: C — Single Continuous World

### 핵심 아키텍처 (콘텐츠 정본 모델 명시)

**Canonical = `/text/{slug}` (정본, SEO·a11y·검색 인덱싱 대상). 월드 안의 표시는 *프레젠테이션*.**

```
┌─────────────────────────────────────────────────────────────────┐
│  Framework: Next.js App Router (Day-0 가정, eng-review에서 잠금) │
│  이유: R3F+WebGPU 프로덕션 사례·MDX·App Router의 layout 영속성 │
│        이 WorldCanvas "지속 마운트" 패턴과 가장 호환.            │
│        (Astro는 islands 모델이 SPA 캔버스 영속과 충돌, View    │
│         Transitions로 우회는 가능하나 R3F context 보존이 별건.) │
│                                                                   │
│  /text/{slug}        ← CANONICAL. 순수 HTML/MDX. 3D 비활성.      │
│                        robots: index, follow. 본문 정본.          │
│  /world              ← 월드 진입점. 카메라 = home waypoint.       │
│  /world/{slug}       ← 같은 월드, 카메라 = post waypoint.         │
│                        <link rel="canonical" href="/text/{slug}">│
│                        월드 안 <Html>은 *프레젠테이션 사본*.      │
│                                                                   │
│  app/layout.tsx (영속 layout)                                     │
│  └─ <WorldCanvas /> ← layout에 마운트, 페이지 전환에 재마운트 X  │
│      ├─ R3F + WebGPURenderer (await renderer.init())              │
│      │   ├─ 폴백 분기: navigator.gpu 미존재 OR init() 실패        │
│      │   │   → WebGL2 Renderer로 자동 대체 (R3F 표준)             │
│      │   ├─ 추가 폴백: prefers-reduced-motion / Low Power Mode    │
│      │   │   감지(배터리·downlink heuristic) → 정적 포스터 이미지 │
│      │   │   + /text/ 라우트로 안내 배너                          │
│      │   └─ 모바일 기본: 정적 포스터, "탐험하기" 버튼으로만 활성  │
│      ├─ <Scene> = 1개의 3D 월드 (장소들 정적으로 배치)            │
│      ├─ <Camera> = 라우트→waypoint 매핑, 스크롤 진행도로 보간     │
│      ├─ <PostPlace>들 = drei <Html> portal로 *제목·발췌만* 표시   │
│      │   (본문 전체 X — 정본 중복 방지. "전체 보기"가 /text/로) │
│      ├─ <UIOverlay> = 리퀴드 글래스 패널 (HTML/CSS backdrop-filter)│
│      └─ <CursorTrail> = 커스텀 커서 (prefers-reduced-motion 존중) │
└─────────────────────────────────────────────────────────────────┘
```

**카메라 안무 라이브러리 — Theatre.js vs GSAP: GSAP로 단일 커밋.** 이유: Theatre.js 2024 이후 유지보수 둔화, GSAP는 안정성·문서·R3F + Lenis 통합 사례 풍부. 두 모델 동시 사용 금지.

### "월드 컨셉" 선택지 (별도 단계 결정)

월드의 비주얼 메타포가 80% 작업. 후보:
- **떠다니는 군도** (Sky Islands) — 각 글·프로젝트 = 섬, 다리·구름·바다로 연결
- **디지털 아틀리에** (작업실 1실) — 책상·책장·캔버스·창밖이 각 콘텐츠
- **데이터 숲** — 글이 자라는 나무, 카테고리가 군락
- **뉴트로 도시** (Y2K Skyline) — 도파민 컬러 네온 빌딩, 픽셀 표지판 = 글 제목

**결정 데드라인:** 다음 office hours 또는 design-shotgun 1회 (1~2주 안).

### "기술 다 넣기" 매핑 (모두 월드 안 정당화)

| 트렌드 | 월드 내 정당화 |
|---|---|
| WebGPU + Three.js + TSL | 월드 자체의 렌더링 엔진. TSL로 셰이더 학습. |
| 모핑 스크롤 (GSAP/Lenis + Theatre.js) | 카메라 안무 + 장소 간 형태 보간. |
| Rive 마이크로 인터랙션 | 월드 안 표지판·버튼·이정표의 반응형 애니. |
| Spline | 월드 안 정적 오브젝트(가구·조각)를 Blender 대신 Spline로 내보내 임포트. |
| 도파민 컬러 | 월드의 라이팅·하늘·네온. 단 **하나의 베이스 톤 위에서**(잡탕 방지). |
| 리퀴드 글래스 | 떠 있는 UI 패널·내비·모달. CSS `backdrop-filter` + 가벼운 셰이더 하이라이트. |
| 뉴트로 / 픽셀 / Y2K | 월드 안 표지판·CRT·픽셀 캐릭터로 격리 (전체 톤 안 잡아먹음). |
| 커스텀 커서 | 월드 좌표 호버로 인터랙션 힌트, 자석 효과. |
| 인지적 접근성 | `/text/{slug}` 폴백 라우트 + 키보드 waypoint 점프 + 모션 줄이기 토글. |

### 강제 게이트 (plan에 박음)

- **성능 예산 (목표치, v0.1에서 측정 후 조정)** — 데스크톱: `/text/` 라우트 LCP ≤ 1.5s (canonical), `/world` 첫 의미 있는 프레임 ≤ 3.0s (WebGPU init + KTX2 디코드 포함, Lighthouse가 canvas LCP를 비신뢰하므로 `performance.mark()` 자체 계측), 인터랙션 시 60fps 유지, draw call ≤ 800. 모바일: 정적 포스터 LCP ≤ 1.8s, 월드 진입은 명시적 사용자 액션 후.
- **에셋 파이프라인** — Draco(지오메트리), KTX2/Basis(텍스처), `gltf-transform` CLI 빌드 단계, `<Suspense>` 단계 로딩. **호스팅 주의**: SharedArrayBuffer 필요 시 COOP/COEP 헤더, KTX2 MIME (`image/ktx2`) 명시. Vercel/CF Pages 둘 다 가능하지만 사전 검증 필요.
- **디자인 토큰** — 색·여백·타이포·이징·라이팅 톤을 *코드화된 토큰 1세트*로. 새 컴포넌트는 토큰 미준수 시 lint fail.
- **a11y 최소선** — 모든 콘텐츠 정본은 `/text/{slug}`(이미 canonical), 키보드만으로 모든 waypoint 도달(Tab 순회 + Enter), `prefers-reduced-motion` 존중(카메라 안무 fade-cut으로 강등), alt 텍스트 사이드카 JSON, 월드 안 텍스트는 SR-only DOM 미러 또는 폴백 라우트로 우회.
- **의존성 동결 (lock-set)** — 아래 셋만 v1.0 출시 전까지 허용. **새 라이브러리 추가는 design doc revision을 거쳐야 함.**
  ```
  Next.js (App Router) | three.js + WebGPURenderer | @react-three/fiber | @react-three/drei
  GSAP (+ ScrollTrigger) | Lenis | zustand | MDX | Tailwind v4 | Rive web runtime
  Spline runtime | gltf-transform (빌드 도구) | (셰이더는 TSL 직접 작성)
  ```
  이 셋 자체가 이미 12개 — feature creep 가드는 *추가 금지*가 본질.

---

## Open Questions

(아래 1·2는 이미 Day-0 가정으로 잠금: 프레임워크 = Next App Router, 카메라 = GSAP. eng-review에서 검증만.)

1. **월드 컨셉 4안 중 어느 것?** — `/sunco:design-shotgun` 또는 `/sunco:design-consultation` 1회로 비주얼 후보 좁히기. *v0.5 진입 전 필수 결정.*
2. **TSL 도입 깊이** — v1부터 자작 셰이더? 아니면 Drei/lamina 프리셋만? (학습 곡선 vs 차별화 트레이드오프) — eng-review에서.
3. **호스팅 검증** — Vercel(Next) / Cloudflare Pages — WebGPU·KTX2 MIME·SharedArrayBuffer COOP/COEP·엣지 캐시. Day-0 검증 항목.
4. **콘텐츠 작성 워크플로우** — MDX 파일 직접 작성 vs Obsidian → 빌드. 일기 빈도 = 마찰 적은 게 우선. (Notion API는 의존성 셋 외라 보류.)
5. **위키 자료 내 미검증 주장** — "WebGPU 15~30배 성능", "팬톤 2026 컬러 'Cloud Dancer'", "Safari 26 WebGPU 완전 지원", "모바일 사용자의 53% 3초 이탈" 등은 **인용 부적절** — Assignment 1번에서 `/sunco:research`. **검증 결과로 핵심 가정이 깨지면 design doc revision.**
6. **법적/윤리적** — 일기 콘텐츠가 본인 외 인물 식별 가능 정보 포함 시 사전 게재 동의·익명화 정책.
7. **R3F + WebGPU 프로덕션 함정** — drei helper의 WebGL assumption (post-processing, raycast 일부) 호환성. eng-review에서 reference 프로젝트 1~2개 확인.

---

## Success Criteria (자체 부과 마일스톤 — 트렌드 가지치기 X, 릴리즈 단계화 O)

주당 8~12h 가정. 외부 강제력 없으므로 마일스톤 데드라인을 **공개 약속**(트위터/지인 공유)으로 잠금.

- **v0.1 — World Skeleton (~2주, 2026-04-28)**: Next App Router + WebGPURenderer init OK + WebGL2 폴백 검증 + 빈 월드 + 카메라 1 waypoint + `/text/{slug}` 라우트 1개 + Vercel/CF 배포. *목표: "동작 증명, 성능·디자인 미정"*
- **v0.5 — Inhabited World (~10주, 2026-06-30)**: 월드 컨셉 확정, 장소 3개, GSAP 카메라 시퀀스, MDX→`<Html>` 발췌+canonical 링크, prefers-reduced-motion 폴백, 디자인 토큰 1세트, 글 1편(일기) 정본. *게이트: 데스크톱 60fps 측정 통과 후에만 v1.0 진입.*
- **v1.0 — Public Launch (~32주, 2026-12-31)**: 글 5편 이상, **모든 합의 트렌드 통합 완료**(WebGPU TSL·모핑 스크롤·Rive·Spline·도파민 컬러·리퀴드 글래스·뉴트로·커스텀 커서·a11y 폴백·인지적 접근성 토글), 성능 게이트 통과, 트위터·LinkedIn·디스코드 공개. *데드라인 절대선.*
- **v1.5+**: 추가 글, 사용자별 카메라 시퀀스, 새 월드 영역 등 — design doc revision 후.

각 마일스톤마다 **친구 3명·교수·멘토(강예은) 중 1명에게 데모 + 피드백**을 강제. v0.1 데드라인 미달 시 **즉시 office hours 재호출** (스코프 재조정 트리거).

**병행 프로젝트 시간 충돌**: MILK 백엔드(yeon-03 포크, 활성), WalkMate, 학기 과제, 멘토 미팅(강예은). 이 블로그는 그 다음 우선순위. 충돌 시 마일스톤 슬립을 *선언*(기록·공개), 침묵 슬립 금지.

---

## The Assignment (다음 액션)

이 design doc 승인 후 **순서대로**:

1. **위키 미검증 주장 1차 출처 확인** — `/sunco:research` 또는 수동. (1~2시간) 잘못된 사실 위에 6개월 일하면 망함.
2. **월드 컨셉 좁히기** — `/sunco:design-shotgun "3D 월드 비주얼 메타포: 떠다니는 군도 / 디지털 아틀리에 / 데이터 숲 / 뉴트로 도시"` (반나절~1일)
3. **기술 스택 잠금** — `/sunco:eng-review` 로 Astro vs Next, R3F + WebGPU 통합 패턴, 호스팅, 에셋 파이프라인 결정. (1일)
4. **v0.1 implementation plan** — `/sunco:plan` 으로 World Skeleton 마일스톤만 우선. (반나절)
5. **첫 코드** — v0.1 구현 시작. **단, 의존성 동결 룰 적용**: 위 명시한 라이브러리 외 추가 금지.

---

## 기록된 위험 (사용자 의식적 수용)

- 단계화 거부 → v1 도달까지 6~12개월 현실적, 학기·MILK·WalkMate와 충돌. 사용자가 트레이드오프 의식하고 선택.
- 무기한 → 자체 마일스톤 외에 외부 강제력 없음. v0.1 데드라인을 어기면 그 시점에 office hours 재호출 권고.
- 바이브 코딩 한계 → 셰이더·perf·a11y는 본인 학습 필요. AI 출력만 받아쓰면 마지막 20%에서 무너짐.
